#include <stdio.h>

/**
 * main -function
 *
 * Description: prints all single digits
 *
 * Return: (0) success
 */

int main(void)
{
	for (a = 0; a <= 10; a++)
		putchar (a);
	putchar ("\n");
	return (0);
}
